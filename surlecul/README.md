# surlecul

Small, obfuscated C program (IOCCC-style). The name is a playful reversal of “le cul sur” (French). It’s a single recursive `main()` that produces output when run; no practical use—purely a curiosity. Build with any C compiler (e.g. Visual C++ project `surlecul.dsp`).
