# Hello

Minimal **child process** for the CreatePipe demo: prints "Hello World!" and a few lines to stdout, does a long loop, then prints again. Used so the parent (CreatePipe) can capture and display child output. Build with `Hello.dsp`; run standalone or as the child launched by the pipe wrapper.
