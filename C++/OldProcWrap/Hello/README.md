# Hello

Minimal **child process** for the OldProcWrap demo: prints via `printf`, `cerr`, and `cout` so the parent can capture stdout/stderr. Used to verify pipe redirection. Build with `Hello.dsp`; run standalone or as the child launched by OldProcWrap.
