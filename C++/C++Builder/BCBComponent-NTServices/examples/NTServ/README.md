# NTServ

Example app for **TNTService** and related NT service components: add, delete, control, and configure Windows NT services from the IDE or this demo. Run after compiling and installing the parent BCBComponent-NTServices package. See the package `readme.txt` for installation steps.
