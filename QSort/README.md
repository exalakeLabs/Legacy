# QSort

Console app that demonstrates **quicksort** on an integer array. Implements recursive `QSort()` (partition and recurse), fills an array, sorts it, and prints results. Built with Visual C++ (MFC/AppWizard console project `testQSort.dsp`).

## Build and run

Open `testQSort.dsp` in Visual C++, build, and run the resulting executable.
