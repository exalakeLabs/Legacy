# testdll_

Output or staging folder for the **testdll** DLL built by the classdll project. The DLL (previously `testdll.dll`) is loaded by the test app or C++Builder Project1. Build the parent **DLLTest** project to produce the DLL; place it here or on the path when running the test executable.
