# winCE Winsock

**Windows CE** Winsock test program. `SOCKTEST.C` and resources target CE; `MAKEIT.BAT` drives the build. Use for testing or porting socket code to CE. Build with a Windows CE SDK and toolchain; run on a CE device or emulator.
