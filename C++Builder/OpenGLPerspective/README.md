# OpenGLPerspective

C++Builder **OpenGL perspective** demo. VCL form app (Project1) that uses OpenGL for 3D rendering with perspective. Build with C++Builder (OpenGL libraries required); run to view the perspective scene.
