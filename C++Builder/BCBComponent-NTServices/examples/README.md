# examples

**Sample applications** for the NT Services components: **NTServ** (service management), **UserMan** (user/group management), and others. Build and run after installing the main NT component package (e.g. NT.bpl / NTSet.bpg). Each subfolder is a standalone demo; see the main **BCBComponent-NTServices** readme for install and path setup.
