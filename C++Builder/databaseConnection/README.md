# databaseConnection

C++Builder **database connection** demo. Provides a form (Unit2) used by DatabaseConnectionTest; demonstrates connecting to a database and simple VCL data UI. Build with C++Builder; often opened as a dependency of DatabaseConnectionTest.
