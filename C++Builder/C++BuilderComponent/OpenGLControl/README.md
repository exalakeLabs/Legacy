# OpenGLControl

**VCL OpenGL control** component for C++Builder: embeds an OpenGL rendering context in a form. Part of the C++BuilderComponent package; build and install the package to use this control on forms. See parent **C++BuilderComponent** README.
