# TOpenGLPanel

**TOpenGLPanel** component for C++Builder: panel control that hosts OpenGL rendering. Part of the C++BuilderComponent package; install the package to use on forms. See parent **C++BuilderComponent** README.
