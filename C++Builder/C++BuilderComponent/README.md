# C++BuilderComponent

Custom **C++Builder component(s)** for the IDE. Includes an OpenGL control (OpenGLControl) and related sources. Build the component package and install into the C++Builder IDE to use the control on forms.
