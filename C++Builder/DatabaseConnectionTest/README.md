# DatabaseConnectionTest

C++Builder app that tests **database connectivity**: creates Form1 and Form2 (from databaseConnection), runs the application. Use to verify DB connection and form integration. Build and run with C++Builder.
